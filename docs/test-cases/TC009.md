# TC009 - As a user signing up, I want validation when my phone is not numeric so that I fix the format before submitting.

## Test case identity

| Field | Value |
| --- | --- |
| **TC ID** | TC009 |
| **Title** | As a user signing up, I want validation when my phone is not numeric so that I fix the format before submitting. |
| **Feature** | Authentication - Sign up (negative) |
| **Test data** | `testData/SignUp.xlsx` (Sheet1, row **`tc_id`** = **TC009**) |
| **Group** | Regression |

## Objective

When the phone value is **non-numeric** (letters only, per **`invalid_phone_number`** on the row), the phone field shows validation text that matches **`expected_message`** (which may span multiple lines in the sheet).

## Preconditions

- The sign-up screen is available.

## Test data

Authoritative field values are in **`testData/SignUp.xlsx`**, Sheet **Sheet1**, row **`tc_id`** = **TC009**. Use **`invalid_phone_number`**, **`focus`**, and **`expected_message`** as defined there.

## Steps

1. Open the **sign-up** page.
2. Enter **first name**, **last name**, **occupation**, **gender**, **email**, and **password** from the row with valid-format values.
3. In the **phone** field, enter **`invalid_phone_number`** from the row (non-numeric / letters-only value for this scenario).
4. Click **Register** / **Sign up**.
5. Read the **phone** field error text and compare it to **`expected_message`** on the row (if the sheet contains multiple lines, match the UI’s combined or stacked text accordingly).

## Expected result

- The phone field error or hint matches **`expected_message`** from the sheet (subject to how the UI formats multiple rules).

## Notes

- Automation mapping (test class, data loading) is filled in when the test script exists; this document describes the scenario only.
